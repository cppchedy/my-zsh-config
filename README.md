# Overview

This repository provides a minimal, reproducible setup for a freshly installed Ubuntu environment.

It currently provisions:

* Zsh as the default shell
* Zsh completion and useful plugins
* Git integration
* fzf
* zoxide
* tmux

Run the provisioning script:

```bash
./scripts/provision.sh
```

The script is designed to be safe to run multiple times.
